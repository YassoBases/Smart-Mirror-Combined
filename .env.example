# Backend API URL — set this when your backend server is running
# REACT_APP_API_URL=http://localhost:3000

# Set to true to show demo emails in the Gmail widget (no backend needed)
REACT_APP_GMAIL_USE_MOCK=true
